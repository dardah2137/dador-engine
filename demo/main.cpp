#include <DadorEngine.hpp>

int main(int argc, char** argv) {
    DadorEngine::Engine engine = DadorEngine::Engine();
    DadorEngine::Window d("test tetetest rgdfbv", 800, 600);
    while (true) { 
        engine = engine;
    }
}